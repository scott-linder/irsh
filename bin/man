#!/usr/bin/fish

if test -n "$argv"
    set file usr/man/(echo $argv | tr -cd '[:alnum:]')
    if test -f $file
        echo $argv (cat $file)
    else
        echo no manual page for $argv
    end
else
    echo available topics: (ls usr/man)
end
