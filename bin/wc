#!/usr/bin/fish

. lib/opts.fish

opts cw $argv

if test -z "$flags"
    set flags -l
end

if test -n "$pos"
    for path in $pos
        wc $flags (lib/path $path) | awk '{ print $1 }'
    end
else
    cat | wc $flags
end
