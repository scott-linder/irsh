#!/usr/bin/fish

if test -n "$argv"
    for file in $argv
        set paths $paths (lib/path $file)
    end
    cat $paths ^/dev/null | shuf -n1; or exit $status
else
    shuf -n1; or exit $status
end
