#!/usr/bin/fish

. lib/irc.fish
. lib/line.fish
. lib/sudo.fish

if sudoer
    for chan in $argv
        set chan (clean_chan $chan)
        join $chan
        line_add $chan var/chans
        mkdir -p var/root/$chan
    end
end
