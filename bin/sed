#!/usr/bin/fish

. lib/opts.fish

opts n $argv

if test (count $pos) -eq 2
    sed $flags -e $pos[1] (lib/path $pos[2])
else if test (count $pos) -eq 1
    sed $flags -e $pos[1]
else
    echo bad commandline
end
