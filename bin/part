#!/usr/bin/fish

source lib/irc.fish
source lib/line.fish
source lib/sudo.fish

if sudoer
    for chan in $argv
        set chan (clean_chan $chan)
        part $chan
        line_del $chan var/chans
    end
end
