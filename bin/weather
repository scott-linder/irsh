#!/usr/bin/fish

. lib/opts.fish

opts Ff:u: $argv

if test (count $pos) -eq 0
    set loc Kalamazoo,US
else if test (count $pos) -eq 1
    set loc $pos[1]
else
    bin/man (basename (status -f))
    exit 1
end

ansiweather -l $loc -u imperial -a no $flags | sed 's/^\s\+\(.*\)/\1/'
