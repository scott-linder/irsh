#!/usr/bin/fish

set api 'http://api.urbandictionary.com/v0/define'
set py 'import json; import sys; print(json.loads(sys.stdin.buffer.raw.readall().decode("utf-8"))["list"][0]["definition"])'

for term in $argv
    curl -s -G $api --data-urlencode "term=$term" | python3 -c $py | head -1
end
